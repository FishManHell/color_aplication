import React from 'react';
import {ErrorText} from "../../styled_components/Error";

const Error = () => {
    return <ErrorText>Sorry, but you don't have a color block</ErrorText>
};

export default Error;
